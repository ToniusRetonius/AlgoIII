#include <iostream>
#include <vector>

using namespace std;

int main()
{   
    /* declaracion de variables */
    int casos;
    int cant_lock;
    int lock;
    vector<int> locks;
    
    /* capturamos */
    cin >> casos;

    cin >> cant_lock;

    return 0;
}